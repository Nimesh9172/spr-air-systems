/**
 * Formatting helpers (dates, currency, display labels).
 * Keep pure and framework-agnostic for reuse in server and client code.
 */

export function formatLabel(value: string): string {
  return value;
}
