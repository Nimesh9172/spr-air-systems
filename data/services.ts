import type { ContentSummary } from "@/types/common";

/**
 * Static service records until a backend data source is connected.
 */
export const services = [] as const satisfies readonly ContentSummary[];
