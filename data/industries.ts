import type { ContentSummary } from "@/types/common";

/**
 * Static industry records until a backend data source is connected.
 */
export const industries = [] as const satisfies readonly ContentSummary[];
