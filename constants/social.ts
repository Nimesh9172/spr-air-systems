import type { SocialLink } from "@/types/site";

/**
 * External company profiles displayed by future navigation and footer UI.
 */
export const SOCIAL_LINKS = [] as const satisfies readonly SocialLink[];
