/**
 * Contact form API service layer.
 * Swap the implementation to call Django endpoints without changing UI callers.
 */

export async function submitContact(): Promise<void> {
  // Intentionally unimplemented — wire to Django contact API later.
}
