/**
 * App-level 404 UI rendered when a route is not found or `notFound()` is called.
 * Replace with branded marketing 404 content when UI work begins.
 */
export default function NotFound() {
  return null;
}
