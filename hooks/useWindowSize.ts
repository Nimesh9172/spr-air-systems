"use client";

/**
 * Viewport size hook for responsive behavior outside of CSS media queries.
 * Implement with resize listeners when needed by layout or sections.
 */
export function useWindowSize() {
  return { width: 0, height: 0 };
}
