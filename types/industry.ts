/**
 * Industry domain types for vertical pages and filtering.
 * Align field names with the future Django industry serializer.
 */

export type Industry = {
  id: string;
  slug: string;
  name: string;
};
